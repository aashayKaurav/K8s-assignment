apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: k8s-assignment
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
